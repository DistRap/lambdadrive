# For more information, see: http://docs.haskellstack.org/en/stable/yaml_configuration.html

# Specifies the GHC version and set of packages available (e.g., lts-3.5, nightly-2015-09-21, ghc-7.10.2)
resolver: lts-8.9

# Local packages, usually specified by relative directory name
packages:
- '.'
- 'client'
- location:
    git: 'https://git.48.io/liveplot/'
    commit: e0a590b6203fd076ea133e34317b6d117a9fab12
  extra-dep: true
- location:
    git: 'https://git.48.io/pipes-serial/'
    commit: f83c7f37509d70f94f12784ca63ce27a5ad46ee3
  extra-dep: true
- location:
    git: 'https://git.48.io/andromeda/'
    commit: 7f1bd272e14c831caa030cd24354e8416b646263
  extra-dep: true
- location:
    git: 'https://git.48.io/mvc/'
    commit: bf1401391096ebfa3089d8f4de2141d2c21ddea7
  extra-dep: true
- location: ../ivory-tower-stm32/
  extra-dep: true
  subdirs:
    - ivory-bsp-stm32
    - ivory-bsp-tests
    - ivory-freertos-bindings
    - tower-freertos-stm32
    - tower-echronos-stm32
- location: ../ivory/
  extra-dep: true
  subdirs:
    - ivory
    - ivory-artifact
    - ivory-backend-c
    - ivory-hw
    - ivory-opts
    - ivory-serialize
    - ivory-stdlib
- location: ../tower/
  extra-dep: true
  subdirs:
    - tower
    - tower-config
    - tower-hal
    - tower-aadl
- location: ../ivory-tower-posix/
  extra-dep: true
- location: ../ivory-tower-hxstream/
  extra-dep: true
- location: ./schema/
  extra-dep: true
  subdirs:
    - odrive-schema-native
    - odrive-schema-tower

# Packages to be pulled from upstream that are not in the resolver (e.g., acme-missiles-0.3)
extra-deps:
- GLUtil-0.9.1.1
- serialport-0.4.6
- vinyl-gl-0.3.0.1
- Vec-1.0.5

# Override default flag values for local packages and extra-deps
flags: {}

# Extra package databases containing global packages
extra-package-dbs: []

# Require a specific version of stack, using version ranges
# require-stack-version: -any # Default
# require-stack-version: >= 1.0.0

# Override the architecture used by stack, especially useful on Windows
# arch: i386
# arch: x86_64

# Extra directories used by stack for building
# extra-include-dirs: [/path/to/dir]
# extra-lib-dirs: [/path/to/dir]

# Allow a newer minor version of GHC than the snapshot specifies
# compiler-check: newer-minor
